# Снимок правил контроллера — 26.09.2026 12:19:32

Источник: фактическое содержимое `/etc/wb-rules` на контроллере объекта «04_22 Даниловский», переданное с объекта в ходе ПНР.

## Принцип хранения

- Файлы в каталоге `wb-rules/` сохранены как фактический снимок контроллера.
- Имена файлов сохранены без служебных суффиксов `(2)` / `(3)`, появившихся только при загрузке файлов в чат.
- Активные и `.disabled` файлы хранятся с их фактическим статусом.
- Этот каталог не является целевой архитектурой проекта и не должен автоматически заменять файлы в `Wirenboard/wb-rules/`.
- Изменения выполняются в рабочем каталоге проекта только после разбора и согласования.

Всего сохранено файлов из `/etc/wb-rules`: **30**.
